<?xml version="1.0" encoding="utf-8"?>

<resources>

    <!-- Base Android theme used after the system splash finishes. -->
    <style
        name="Theme.PJDevMultiverseApp"
        parent="android:style/Theme.Material.Light.NoActionBar">

        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

    </style>

    <!-- Temporary starting theme displayed before Compose is ready. -->
    <style
        name="Theme.PJDevMultiverseApp.Starting"
        parent="Theme.SplashScreen">

        <item name="windowSplashScreenBackground">
            @color/splash_background
        </item>

        <item name="windowSplashScreenAnimatedIcon">
            @drawable/ic_launcher_foreground
        </item>

        <item name="postSplashScreenTheme">
            @style/Theme.PJDevMultiverseApp
        </item>

    </style>

</resources>